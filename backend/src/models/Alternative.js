export default class Alternative {
    constructor(questionId, description, correct) {
        this.questionId = questionId
        this.description = description
        this.correct = correct
    }
}